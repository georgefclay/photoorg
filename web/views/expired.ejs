<%- include('partials/head', { title: 'Link expired' }) %>
<h1>This link has expired</h1>
<p>The link you followed is no longer valid. Sign-in links last 15 minutes and can only be used once.</p>
<p>If you were trying to sign in, <a href="/login">ask for a fresh link</a>.</p>
<%- include('partials/foot') %>
